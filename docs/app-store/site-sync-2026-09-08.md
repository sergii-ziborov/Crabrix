# Website and README consistency check — 8 September 2026

The six principal pages (`/`, `/about`, `/technology`, `/privacy`, `/terms`,
`/support`) were fetched from both crabrix.com and crabrix.lovable.app.
Every page loaded successfully. Their visible body text matches the canonical
`site/*.html` copy after HTML entity and whitespace normalization and exclusion
of scripts/styles. The same six source files fetched from GitHub at native
commit `b897f2e52e42db9e60d801cfcb98f4ce486a1fad` match the local files byte for byte.

Lovable's HTML differs in presentation: canonical metadata, light-mode fixes
and AVIF/WebP screenshot delivery. These are intentional, not copy divergence.
Privacy, terms, support, product counts and compiler limitations agree.

The native README now states archive checksums are verified before extraction
or trust, matching the website wording. The deployment guide now explains
visible-text comparison instead of requiring full-file byte equality.

Lovable's generic README was replaced with product-specific documentation,
including the native app's build-5 Waiting for Review status, manual release,
419 native simulator tests, source links, website commands and hosting split.
Lovable reported a successful production build for the README update.

The custom domains still serve Cloudflare; crabrix.lovable.app serves Lovable.
No domain migration or automatic GitHub connection for the Lovable website is
claimed. This check verifies GitHub's native application repository and its
canonical website copies independently of the Lovable project repository.

The five legacy aliases `/about.html`, `/technology.html`, `/privacy.html`,
`/terms.html`, `/support.html` returned HTTP 200 on Lovable. `/index.html`
returned 404; the canonical home route is `/`.

This was a content and deployment consistency check. Native binaries were not
changed or rebuilt, and the App Store review submission was not modified.
